export type Memecoin = {
    image: string;
    name: string;
    address: string;
    creator: string;
    time: string;
    description: string;
    marketCap: string;
};

export type NewMeme = {
    name: string;
    description: string;
    image: string;
}